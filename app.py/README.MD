# Product CRUD (Python, in-memory)

CLI para CRUD de produtos em memória. Ideal para treinar fundamentos de Python e arquitetura simples (domínio + repositório + CLI).

## Como executar
```bash
python app.py
# ou no Windows:
py app.py